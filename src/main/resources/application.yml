server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mbtisns
    username : root
    password: 12345789a
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
