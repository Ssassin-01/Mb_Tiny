server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mbtisns
    username : root
    password: 1234
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
